# ─────────────────────────────────────────────────────────
#  Smart Search Frontend — Environment Variables Template
#  Copy this file to .env.local and fill in real values.
#  ⚠️  NEVER put secrets or tokens in VITE_ variables —
#      they are bundled into the public JS bundle.
# ─────────────────────────────────────────────────────────

# URL of the Rails API backend
VITE_API_URL=http://localhost:3000
